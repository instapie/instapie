= form_tag do
  .field
    = label_tag(:email)
    = text_field_tag(:email)

  .field
    = label_tag(:password)
    = password_field_tag(:password)

  .actions
    = submit_tag('Log In')
    = back_link(http_url('root'))
